package me.swishhyy.swishhyysAdditions.commands.admin;

public class GiveCommand {
    // Has a permission check for sa.admin this is a permission granting command usage for any command in the commands/admin package
    // Head command for many sub commands for eg: /sa give {player_name (if no playername is given then give to the person running the command)} gcrystal
    // this command will give the player a growing crystal or any item from within the items package! possibly a dynamic scan of that package to find all items
    // so i dont have to manually add them to the command class
}
